readable = "readable variable in readable/readable.py"

# test traverse_assign_node() get_local_variables() part which fails if parent is a module
_level = "module"
module_level_variable = f"{_level} level variable"